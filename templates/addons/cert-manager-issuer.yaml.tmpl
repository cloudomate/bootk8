# Cert-Manager ClusterIssuers
# A self-signed root CA is bootstrapped automatically.
# To use Let's Encrypt instead, replace the 'letsencrypt' issuer below with your
# ACME credentials and remove the selfSigned issuer from your Certificate specs.
---
# Self-signed issuer — used to mint the root CA certificate below.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned
spec:
  selfSigned: {}
---
# Root CA Certificate — issued by the self-signed issuer.
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cluster-root-ca
  namespace: cert-manager
spec:
  isCA: true
  commonName: ${CLUSTER_NAME}-ca
  secretName: cluster-root-ca
  duration: 87600h    # 10 years
  renewBefore: 8760h  # renew 1 year before expiry
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
    group: cert-manager.io
---
# Cluster CA issuer — use this in Certificate resources across all namespaces.
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: cluster-ca
spec:
  ca:
    secretName: cluster-root-ca
